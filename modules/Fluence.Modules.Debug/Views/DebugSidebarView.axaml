<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="using:Fluence.Core.Services.Localization"
             xmlns:models="using:Fluence.Modules.Debug.Models"
             xmlns:vm="using:Fluence.Modules.Debug.ViewModels"
             x:Class="Fluence.Modules.Debug.Views.DebugSidebarView"
             x:DataType="vm:DebugSidebarViewModel"
             Background="Transparent">

    <StackPanel Spacing="10"
                Margin="10">
        <Border Padding="10"
                Classes="idePanel">
            <StackPanel Spacing="4">
                <TextBlock Text="{Binding SessionTitle}"
                           FontFamily="{StaticResource FluenceFontFamily}"
                           FontSize="{StaticResource FluenceFontSizeSmall}"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource FluenceBrushTextPrimary}"/>
                <TextBlock Text="{Binding Architecture}"
                           FontFamily="{StaticResource FluenceFontFamily}"
                           FontSize="{StaticResource FluenceFontSizeSmall}"
                           Foreground="{StaticResource FluenceBrushTextSecondary}"/>
                <TextBlock Text="{Binding Status}"
                           FontFamily="{StaticResource FluenceFontFamily}"
                           FontSize="{StaticResource FluenceFontSizeSmall}"
                           Foreground="{StaticResource FluenceBrushTextSecondary}"/>
            </StackPanel>
        </Border>

        <StackPanel Orientation="Horizontal"
                    Spacing="4">
            <Button Width="36"
                    Height="36"
                    Padding="0"
                    Classes="ideIcon"
                    Command="{Binding ContinueCommand}"
                    IsEnabled="{Binding CanControlExecution}"
                    ToolTip.Tip="{Binding [Debug.ToolTip.Continue], Source={x:Static loc:Locale.Current}}">
                <Path Data="{x:Static models:DebugToolbarIconCatalog.ContinueIcon}"
                      Width="18"
                      Height="18"
                      Stretch="Uniform"
                      Fill="{StaticResource FluenceBrushTextPrimary}"/>
            </Button>
            <Button Width="36"
                    Height="36"
                    Padding="0"
                    Classes="ideIcon"
                    Command="{Binding StepOverCommand}"
                    IsEnabled="{Binding CanControlExecution}"
                    ToolTip.Tip="{Binding [Debug.ToolTip.StepOver], Source={x:Static loc:Locale.Current}}">
                <Path Data="{x:Static models:DebugToolbarIconCatalog.StepOverIcon}"
                      Width="18"
                      Height="18"
                      Stretch="Uniform"
                      Fill="{StaticResource FluenceBrushTextPrimary}"/>
            </Button>
            <Button Width="36"
                    Height="36"
                    Padding="0"
                    Classes="ideIcon"
                    Command="{Binding StepIntoCommand}"
                    IsEnabled="{Binding CanControlExecution}"
                    ToolTip.Tip="{Binding [Debug.ToolTip.StepInto], Source={x:Static loc:Locale.Current}}">
                <Path Data="{x:Static models:DebugToolbarIconCatalog.StepIntoIcon}"
                      Width="18"
                      Height="18"
                      Stretch="Uniform"
                      Fill="{StaticResource FluenceBrushTextPrimary}"/>
            </Button>
            <Button Width="36"
                    Height="36"
                    Padding="0"
                    Classes="ideIcon"
                    Command="{Binding StepOutCommand}"
                    IsEnabled="{Binding CanControlExecution}"
                    ToolTip.Tip="{Binding [Debug.ToolTip.StepOut], Source={x:Static loc:Locale.Current}}">
                <Path Data="{x:Static models:DebugToolbarIconCatalog.StepOutIcon}"
                      Width="18"
                      Height="18"
                      Stretch="Uniform"
                      Fill="{StaticResource FluenceBrushTextPrimary}"/>
            </Button>
            <Button Width="36"
                    Height="36"
                    Padding="0"
                    Classes="ideIcon"
                    Command="{Binding StopCommand}"
                    IsEnabled="{Binding CanRestartSession}"
                    ToolTip.Tip="{Binding [Debug.ToolTip.Stop], Source={x:Static loc:Locale.Current}}">
                <Path Data="{x:Static models:DebugToolbarIconCatalog.StopIcon}"
                      Width="18"
                      Height="18"
                      Stretch="Uniform"
                      Fill="{StaticResource FluenceBrushTextPrimary}"/>
            </Button>
            <Button Width="36"
                    Height="36"
                    Padding="0"
                    Classes="ideIcon"
                    Command="{Binding ReloadCommand}"
                    IsEnabled="{Binding CanRestartSession}"
                    ToolTip.Tip="{Binding [Debug.ToolTip.Reload], Source={x:Static loc:Locale.Current}}">
                <Path Data="{x:Static models:DebugToolbarIconCatalog.ReloadIcon}"
                      Width="18"
                      Height="18"
                      Stretch="Uniform"
                      Fill="{StaticResource FluenceBrushTextPrimary}"/>
            </Button>
        </StackPanel>

        <Border Padding="10"
                Classes="idePanel">
            <StackPanel Spacing="8">
                <TextBlock Text="{Binding [Debug.Label.Variables], Source={x:Static loc:Locale.Current}}"
                           FontFamily="{StaticResource FluenceFontFamily}"
                           FontSize="{StaticResource FluenceFontSizeSmall}"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource FluenceBrushTextPrimary}"/>
                <ItemsControl ItemsSource="{Binding Variables}"/>
            </StackPanel>
        </Border>

        <Border Padding="10"
                Classes="idePanel">
            <StackPanel Spacing="8">
                <TextBlock Text="{Binding [Debug.Label.CallStack], Source={x:Static loc:Locale.Current}}"
                           FontFamily="{StaticResource FluenceFontFamily}"
                           FontSize="{StaticResource FluenceFontSizeSmall}"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource FluenceBrushTextPrimary}"/>
                <ItemsControl ItemsSource="{Binding StackFrames}"/>
            </StackPanel>
        </Border>

        <Border Padding="10"
                Classes="idePanel">
            <StackPanel Spacing="8">
                <TextBlock Text="{Binding [Debug.Label.Breakpoints], Source={x:Static loc:Locale.Current}}"
                           FontFamily="{StaticResource FluenceFontFamily}"
                           FontSize="{StaticResource FluenceFontSizeSmall}"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource FluenceBrushTextPrimary}"/>
                <ItemsControl ItemsSource="{Binding Breakpoints}"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
